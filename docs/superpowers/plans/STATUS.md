# 计划状态说明（2026-07-09）

本目录三份计划（assignee-model-2d、bpmn-designer-assignee-parity、bpmn-designer-full-parity）是 SDD 执行台账，其中大量勾选框虽标为未完成 `[ ]`，但**对应工作大多已随 commit 落地**（见 git log：`3a2e246`、`524c998`、`654cce5`、`05a504f` 等直接对应这些步骤）。

- 这些台账**不再作为活跃 backlog**；真正的后续工作以 `docs/remediation-plan.md` 与 `docs/design/next-gen-workflow-and-formula.md` 为准。
- 其中 BPMN 设计器相关计划已被「用 react-flow 自研替换 bpmn-js」的新方向取代（见设计文档第一部分）。
- 如需核对某条是否已实现，以代码与 `docs/api-contract.md` 为准，而非本目录勾选框。
